# WorkSync Shared

WorkSync의 웹(Web)과 모바일(Mobile) 프로젝트에서 공통으로 사용하는 타입 정의와 유틸리티 함수들을 포함합니다.

## 📂 Structure

- `types/`: 공통 인터페이스 및 타입 정의 (TypeScript)
- `utils/`: 공통 로직 (암호화, 유효성 검사 등)

## 🛠 Key Utilities

### 1. Crypto (`utils/crypto.ts`)
- AES-256-GCM 암호화/복호화
- PBKDF2 키 파생 (Web Crypto API 및 Noble Ciphers 호환)
- Zero-Knowledge 아키텍처 지원

### 2. Validation (`utils/validation.ts`)
- 마스터 비밀번호 복잡도 검증
- 웹과 모바일에서 동일한 보안 정책 강제

## 🔄 How to Sync
- 현재 수동 복사 또는 심볼릭 링크 방식을 사용 중입니다.
- 파일 수정 시 웹과 모바일 양쪽의 동작을 모두 확인해야 합니다.
